# Wayfinder Map: FPL Gaffer

Label: wayfinder:map · Charted 2026-08-16 · Spec: [issue #1](https://github.com/ropats16/fpl-pi-manager/issues/1) · Tickets: [GitHub issues #2–#23](https://github.com/ropats16/fpl-pi-manager/issues), sub-issues of #1 with native blocked-by edges and `wayfinder:<type>` labels. Frontier = open, unblocked, unassigned.

## Destination

A **build-ready blueprint** for the autonomous FPL gaffer on the Pi 4B (2GB): every architectural decision locked (agent runtime, LLM endpoint, Telegram flow, approval flow, actuator approach, repo layout, stage order) and a spec + staged roadmap another session can execute without further decisions.

## Notes

- System shape: Python math pipeline (collector → projections → ILP optimizer) + LLM "gaffer" via Telegram; cron wakes 1–3x/day + wake-on-message; approval mode — no FPL write without explicit Telegram "yes".
- LLM brain remote (OpenAI-compatible endpoint); all context/memory on the Pi as files; secrets (bot token, API key, FPL cookie) Pi-only.
- Standing decisions from charting: **pragmatism-first** runtime choice; **~$5/mo** API ceiling (lean prompt assembly is a design constraint); **this repo = source of truth, Pi pulls**; actuator **in scope, feasibility-gated**; **GW1 pre-deadline run in scope as task work** (deadline Fri 2026-08-21 17:30 UTC).
- Security standing rules (from claw research): memory-poisoning of persona/memory MD files is demonstrated; SKILL.md = executable code; allowlist own Telegram user ID; keys outside agent-readable workspace; no exposed ports (long-polling only).
- Facts: Pi host `fplpi`, user `saf`; FPL entry id in gitignored `.env` (`FPL_ENTRY_ID`, see `../.env.example`); prior-effort spec at [fpl-agent-system.md](../fpl-agent-system.md).
- Skills per ticket type: /grilling for grilling tickets, /research for research, /prototype when the visualizer graduates.

## Decisions so far

- Charting session (2026-08-16) — destination = build-ready blueprint; pragmatism-first runtime bias; ~$5/mo budget; git-repo-Pi-pulls dev flow; actuator in-scope feasibility-gated; GW1 run in-scope as tasks.
- [Claw/runtime landscape research](research/claw-research-2026-08.md) — PicoClaw primary candidate (Go, 10–20MB, markdown-native workspace, native Kimi/Qwen, Telegram long-polling, cron); ZeroClaw strong alt (security/velocity); ~500-line Python DIY viable; OpenClaw/Hermes/NanoClaw/TinyAGI ruled out on 2GB; resident daemon idles at ~1% RAM so scheduled-wake-vs-24/7 is a non-question; Kimi K2.5 ($0.60/$3.00 per 1M) or qwen-plus fit the budget, both with function calling.
- [Mac folder audit](research/mac-folder-audit-2026-08-16.md) — folder was a stale flattened chat-download, not a codebase; `fpl_optimizer.py` exists nowhere on the Mac (Pi or lost); prior agent's inventory partly fabricated (`draft_board.py`, `MASTER-PLAN.md` never existed); local pipeline can't run end-to-end (stale `fpl_api.py` lacks `csv` mode; projections selftest failing); `season-state.json` never initialized; no secrets present; PII (entry ID, `saf@fplpi`) in plaintext — scrub or keep repo private.
- [Pi inventory + recovery](research/pi-inventory-2026-08-16.md) ([#2](https://github.com/ropats16/fpl-pi-manager/issues/2)) — **nothing lost**: `fpl_optimizer.py`, csv-mode `fpl_api.py`, and `draft_board.py` all recovered from `~/fpl/` into the repo (audit's "missing/fabricated" claims were wrong). Pi = Pi 4B, Debian 13 trixie (not Lite), aarch64, Python 3.13.5, 1.8 GiB RAM, 21 G free. **No cron exists anywhere** and `logs/` is empty — claimed nightly 03:30 never ran. Data 11 days stale (2026-08-05, GW1–6); fresh fetch needed before GW1 run. Host resolves via `fplpi.local`, key auth installed.
- Spec + tickets published (2026-08-16) — the spec is [issue #1](https://github.com/ropats16/fpl-pi-manager/issues/1); 22 tickets (#2–#23) cut from spec + map with native sub-issue parenting, blocked-by dependencies, and type labels. Open tickets are found by query on the tracker, not listed here. Deadline-critical chain: #2→#3→#4→#5 before Fri Aug 21 17:30 UTC.
- Tree consolidated ([#3](https://github.com/ropats16/fpl-pi-manager/issues/3), 2026-08-16) — repo is the single authoritative tree: all four `.py` re-verified byte-identical (sha256) to `saf@fplpi.local:~/fpl/`, zero divergence. Killed the flattened chat-download cruft: `rm _index.md` (dead memory-index), fixed broken links in `fpl-agent-system.md` + `SKILL.md`. **PII decision (revised same day):** repo stays private for now, but the entry ID was moved out for eventual public safety. Correction: the entry ID is **not** in any `.py` — it's a runtime `--entry` CLI arg — so it only ever sat in docs. Now homed in gitignored `.env` (`FPL_ENTRY_ID`) with committed `.env.example` placeholder; scrubbed the two doc literals (id + team name — both link back to the real name via the public entry API). Residual, not yet scrubbed: Pi host/user (`saf@fplpi`, LAN-only mDNS, low risk) and git history (retains prior literals; not worth rewriting for low-risk public data). Skill-file shape (single vs multi-agent, one-skill-per-task) deferred to the [#9](https://github.com/ropats16/fpl-pi-manager/issues/9) architecture grilling, not settled here.
- [GW1 quick-selection run](gw1/index.md) ([#25](https://github.com/ropats16/fpl-pi-manager/issues/25), 2026-08-16/17) — one-shot multi-agent gaffer run producing the reviewed GW1 2026-27 brief before the Fri 2026-08-21 17:30 UTC deadline. Parent gaffer + 5 angle sub-agents (Opus 4.8, Opus-4.8 leaves, tight-ship anti-fabrication) — availability/minutes · fixtures/odds · talent/style/class-prior · value/EO · optimizer-audit — then an assistant-manager pushback loop the gaffer adjudicated (Rohit = observer). **Squad (£100.0m, 3-4-3):** Raya; Gabriel, Mitchell, Shaw; Bruno(MUN), Mbeumo, Szoboszlai, Yates; **Haaland(C)**, João Pedro, Calvert-Lewin; bench Palmer(GK)/Diop/Hughes/van Ewijk (autosub order); **(VC) Bruno**. Key calls: only two premiums fit (Haaland+Bruno); Arsenal taken for the **clean sheet not the attack** (attack minefield — Saliba/Timber out, Gyökeres benched); optimizer **overruled** (thin last-season model, 450-min floor hides promoted/new). **Premise correction assimilated mid-run:** the "corrupted local data" flag was falsified — the team set is correct 2026-27 (Coventry/Hull/Ipswich promoted; Salah & Son left the PL); the real gap is the projection model. Live official FPL API + live web (odds/team-news) were authoritative. Output: [brief.md](gw1/brief.md) + [decision-log.md](gw1/decision-log.md); baseline for post-GW review ([#21](https://github.com/ropats16/fpl-pi-manager/issues/21)). Not necessarily final — amendable before deadline. Deadline re-checks flagged (Shaw fitness→Kayode, Hughes identity, Yates start, scorer-odds re-pull ~20-21 Aug).
- [Team-selection research wiki](research/team-selection/index.md) ([#24](https://github.com/ropats16/fpl-pi-manager/issues/24), 2026-08-16) — evidence-backed survey of what drives FPL selection, built from 11 Opus-4.8 background runs (4 seed clusters + 3 open-ended discovery lenses + 4 gap follow-ups). Output is an **LLM-maintained wiki** (Karpathy convention, per [gw1/approach.md](gw1/approach.md)), not a monolith: [importance-ranking](research/team-selection/importance-ranking.md) (all axes seed+discovered, method-first, evidence-tagged), factor/method/source pages, immutable [`raw/`](research/team-selection/raw/) snapshots. **Discovery mattered**: the seed axes were a floor not a ceiling — open-ended agents surfaced rank/effective-ownership as the true objective, whole-XI covariance, chip *timing* (~+49 pts/yr), team-style/usage share, DEFCON (2026/27 rule), decision-timing option value, and a "sounds-smart-no-signal" list (possession%, weather, kickoff time, new-manager-bounce). Concrete deliverables for the GW1 run: the [class-player prior](research/team-selection/class-player-prior.md) (Bayesian shrinkage, applicable), preferred [data sources](research/team-selection/sources/data-sources.md) for odds/friendlies/H2H/availability, and a [signal-synthesis](research/team-selection/methods/signal-synthesis.md) method (gate-then-pool, near-equal weights not learned, calibration, decision-log template). **Evidence gate** applied per Rohit: adopt FPL methods wholesale only with documented results (OpenFPL, sertalpbilal solver = proven; established stats = standard; folklore = evaluate-first). The gaffer stays free to overrule any finding — briefing, not rulebook.
- **LLM endpoint + routing locked** ([#8](https://github.com/ropats16/fpl-pi-manager/issues/8) grilling, 2026-08-18) — **endpoint = OpenRouter** (one OpenAI-compatible `base_url` + one key), chosen for model-swap flexibility and per-role model mixing over the cheapest path; the DIY daemon's single LLM seam stays trivial. **Default workhorse = Kimi K2.5** (~$0.60/$3.00 per 1M via OpenRouter); **escalation / A-B target = GPT-5.4** ($2.50/$15.00; GPT-5.5 at $5/$30 only if a proven-budget case appears — it blows $5/mo on any non-trivial usage). **Cache caveat accepted:** Moonshot's ~83% prefix-cache discount ($0.10/1M cached direct) does **not** reliably pass through OpenRouter (tested nil on K3) — forfeited, but worth only cents/mo at single-agent volume, so it doesn't justify a second direct-Moonshot key; OpenAI's caching ($0.25/1M) *does* survive OpenRouter, helping GPT-5.4. **Cost finding (the headline):** model choice is **not** the budget driver — **multi-agent fan-out is.** Lean ~25k-in/2k-out wake ≈ $0.02 (Kimi) / $0.05 (GPT-5.4 cached) single-agent; at ~2 scheduled wakes/day Kimi single-agent ≈ **$1.3/mo ✅**, GPT-5.4 ≈ $2.9/mo ✅, but a 6-agent fan-out per wake ≈ $7–17/mo ❌ on *any* model. **Routing locked = cheap-default + escalation** (direction), implemented as **per-call model selection through the one OpenRouter seam** (caller/job names its `model=`; **no runtime self-upgrade magic** — untestable + silent budget burn); Kimi K2.5 default, GPT-5.4 escalation. **Role→model map deferred to [#9](https://github.com/ropats16/fpl-pi-manager/issues/9)** (it's a function of agent shape) with **provisional defaults for both branches:** single-agent → one capable model (Kimi K2.5, escalate the weekly squad-lock to GPT-5.4); multi-agent → assistants/Scout/angle-briefs on Kimi K2.5, **gaffer** (adjudicator/final call) on GPT-5.4 (maybe 5.5), mapping onto the [#25](https://github.com/ropats16/fpl-pi-manager/issues/25) parent-plus-5-angles pattern. **Cost guardrail handed to #9:** the cheap-assistants/premium-gaffer split is affordable *only* if fan-out is bounded (few agents, not-every-wake, lean context) — #8 sets the $5 ceiling, #9's architecture decision must carry a cost estimate that fits inside it. **Key storage locked = out-of-workspace, Pi-only, injected into the daemon *process* (never model context)**, per the standing "keys outside agent-readable workspace" rule (poisoned-memory/injection exfil is the demonstrated attack). Mechanism target = **systemd `LoadCredential=`** (decrypts into service-private tmpfs `$CREDENTIALS_DIRECTORY`, off the workspace disk *and* out of the user-readable process env that a plain `EnvironmentFile` leaks via `/proc/<pid>/environ`; #7 already locked systemd as supervisor); out-of-workspace root-owned `EnvironmentFile` = acceptable fallback; exact hardening (mode/ownership/rotation) → [#10](https://github.com/ropats16/fpl-pi-manager/issues/10). `FPL_ENTRY_ID` stays in the repo's gitignored `.env` (public data); the three real secrets (OpenRouter key, Telegram token, FPL cookie) do **not** — `.env.example` corrected to say so. **Deferred, not decided here:** role→model map ([#9](https://github.com/ropats16/fpl-pi-manager/issues/9)); secret-store hardening mechanism ([#10](https://github.com/ropats16/fpl-pi-manager/issues/10)). Feeds #9 (agent shape must fit the routing seam + $5 ceiling).
- **Runtime locked: minimal DIY Python daemon** ([#7](https://github.com/ropats16/fpl-pi-manager/issues/7) grilling, 2026-08-18) — chose a ~500-line custom Python daemon over adopting a claw framework; **Hermes dropped** (OOM risk on 1.8 GiB usable, heaviest Py+Node+ffmpeg stack, refuses <64k-ctx models — buys nothing a single-user cron+long-poll agent uses); PicoClaw/ZeroClaw **not adopted but their conventions borrowed** (both were *active* as of the research — Pico v0.3.1 3 Jul, Zero v0.8.4 2 Aug — the "outdated" instinct was the coding-agent hype cycle, not death). **Rationale (priority order):** (1) whole repo is already Python with an offline-selftest idiom; (2) the daemon workload is genuinely ~500 lines and mostly *custom* domain logic (multi-agent gaffer, Scout, chip engine, learning loop), not stock claw behavior — the claw ecosystem optimizes for a personal-assistant-that-remembers, which this isn't; (3) testability is strictly better when you own the HTTP edges; (4) smallest attack surface — no untrusted-skill plugin system to defend (sidesteps the demonstrated SOUL/MEMORY.md poisoning + "SKILL.md → shell in 3 lines" risks, which are framework-shape risks not data risks). **Runtime-skeleton conventions locked here (only):** context **assembled from markdown at runtime** (nothing hardcoded — serves the ~$5/mo lean-prompt ceiling *and* testability); **state = JSON** (`season-state.json`, write-back invariant) / **narrative memory = Karpathy LLM-wiki markdown** (repo's existing house style); a **jobs config** (cron-shaped) with **explicit `deliver` semantics** so a wake that should message can't fail silently (PicoClaw's documented footgun); avoid the `AGENTS.md` filename collision (repo README vs claw rules file). **Activity shape** (entailed by "daemon", reaffirmed from charting): resident process = always-on Telegram long-poll (wake-on-message) **+ cron scheduled wakes 1–3×/day**, `systemd Restart=always`. **HTTP-edge testability — confirmed holds** (acceptance criterion): three mockable seams — **LLM** (OpenAI-compatible base_url), **Telegram** (long-poll), **FPL write/actuator** (fake-by-default until feasibility-proven); an offline **`daemon selftest`** drives the whole wake→brief→approve→act loop with zero network, mirroring `run_pipeline.sh offline`; the approval gate (no FPL write without explicit Telegram "yes") lives in the glue *above* the actuator seam. DIY makes this hold *better* than any framework (a compiled binary seals these edges). **Trial-vs-commit: commit-now** (no framework spike). **Exit strategy:** abandoning DIY costs ~rewrite of the 500-line glue; everything valuable — persona/memory markdown, the file *set*, the whole math pipeline, decision-log format, prompts — is **portable files, zero lock-in**; keeps PicoClaw as a cheap escape hatch, inverting the research's "trial-first" path. **Deferred, not decided here:** LLM endpoint/model ([#8](https://github.com/ropats16/fpl-pi-manager/issues/8) — Kimi K2.5 / qwen-plus / **GPT-5.4/5.5 candidates to A/B via OpenRouter**); persona/workspace/memory file *set* + single-vs-multi + prompt-assembly size bound ([#9](https://github.com/ropats16/fpl-pi-manager/issues/9)); security *mechanisms* ([#10](https://github.com/ropats16/fpl-pi-manager/issues/10)); exact schedule/supervision/Pi-pull cadence ([#11](https://github.com/ropats16/fpl-pi-manager/issues/11)); build deps + first live loop ([#15](https://github.com/ropats16/fpl-pi-manager/issues/15)). Unblocks #9/#10 (and #11/#15 once their other blockers clear).
- **Gaffer architecture locked** ([#9](https://github.com/ropats16/fpl-pi-manager/issues/9) grilling, 2026-08-18) — **one boss + helpers**: ONE gaffer holds every decision in a single continuous context; evidence-gathering fans out to **stepwise** assistant roles writing bounded reports to shared files (the blackboard/stigmergy regime — literature-canonical, and exactly the #25 win pattern made durable). **Roster: 4 weekly analysts** (availability · fixtures/odds · quality/style · market) **+ daily Scout + weekly assistant-manager (AM)**; the optimizer/maths pipeline is a *tool* with occasional audits, not a seat; season jobs (chip calendar, post-GW self-review) are the gaffer's own wakes. Research basis: [gaffer-architecture wiki](research/gaffer-architecture/index.md) — fresh 3-track Opus-4.8 pass (agent-org evidence · season-long machinery · adversarial reference-systems audit), honoring the floor-not-ceiling mandate; two lint corrections fed back to the #24 wiki (chip-timing +49 → tier-2 vendor-only; arXiv 2505.02170 is single-GW). Core evidence: multi-agent pays only for parallel read-heavy gathering (~15× tokens; 80% of the measured gain is token spend); one coherent squad decision favors a single context; debate meshes lose to one adversarial pass. **AM charter:** one evidence-cited challenge pass per GW pre-lock, forced concessions (no manufactured disagreement), no other duties. **Tight ship (charter-level):** coverage contracts (sources checked; explicit "searched-X-found-nothing"; unverified tags), **fact-vs-judgment separation** (facts cite sources, judgments are declared as judgments — never blurred), Scout charter treats a missed story as a failure, gaffer spot-checks and may order re-runs/escalations. **Role→model map (closes #8's deferral):** Kimi K2.5 for analysts/Scout/routine wakes; **GPT-5.4 for the weekly lock decision + the AM challenge** (different-family judge defeats self-preference bias); est. **$2–2.5/mo ✅** within the $5 ceiling, headroom for deadline escalations. **Files:** `agent/` tree — `GAFFER.md` (persona + standing orders) · `roles/*.md` (one small persona per role) · `playbooks/` (per-task how-tos, loaded on demand) · `memory/MEMORY.md` (capped index) + wiki — plus immutable per-GW `reports/gwNN/`; `AGENTS.md` filename avoided per #7. **Prompt assembly:** lean index-then-fetch; always-in = persona + season snapshot + memory index + today's playbook + report index; **hard cap ~25k tokens in, asserted by tests** (#16 AC); critical facts at prompt edges (measured context rot: sub-50% at 32k). **Edit rights:** helper role files change only via gaffer-proposes → Rohit-approves on Telegram (same gate as transfers); the gaffer's own **override rulebook self-amends without Rohit** — 3 scored precedents + AM adversarial review + next-GW activation, git-committed; an **immutable core** (not-valid list, logging duty, scoring loop) is enforced in daemon code and only Rohit can change it. **Judgment authority — supreme but scored:** the maths plan is the baseline; 5 valid override situations (bad/stale input data · hard late news · soft minutes/rotation reads · maths-declared near-ties decided on context · calendar shifts) + a not-valid list (points-chasing, gut-feel against intact underlying stats, price-rise chasing); exceptional out-of-list overrides need AM concurrence to take effect; every override logs situation + cited evidence, is scored post-GW, lessons go to memory, and a lucky win does **not** lower the evidence bar (monthly review judges the pattern). **Cadence:** Scout daily 10:00 IST (post overnight UK price change), appending to a running `reports/gwNN/scout-log.md`; analysts run stepwise ~T−72h reading the Scout's accumulated log; final lock wake T−30..15 min (deadline = first kickoff − 90 min; confirmed XIs drop kickoff−75min, i.e. never pre-deadline — odds carry the leaked news); post-GW review T+24h; monthly chips/calibration; on-event wakes (cup results, price flags, urgent Scout flags on owned/targets). **Objective:** maximize **P(finishing 1st overall)** — title-chasing with staged variance (strongest EV engine + earned differentials/stacking, boldness scaled by rank state); milestones are gauges not goals (beat optimal set-and-forget ~2,500 · top-100k ≈ academic-bot SOTA · top-10k reach; no reference system anywhere publishes a verifiable live rank — assume survivorship bias in all self-reports). **Resolves fog:** Scout = distinct daily role (below, removed); #9 fresh-discovery caveat satisfied. **Deferred:** memory-write/poisoning mechanisms → #10 (file set + write paths fixed here; K2-family shown ~95% poisonable — reviewed writes required); workspace build + prompt assembler → #16; weekly cycle/approval protocol → #12.
- **Security hardening locked** ([#10](https://github.com/ropats16/fpl-pi-manager/issues/10) grilling, 2026-08-18) — every standing rule now has a mechanism on the DIY daemon; full build spec in [security-hardening.md](security-hardening.md). Frame: **assume the model IS injected** (K2-family ~95% poisonable) — guarantees live in plumbing the model can't edit; worst case under this posture = bad advice + dirty files in allowed paths, never code-exec/secret-theft/silent team changes. **① Tools:** fixed hand-written registry, **no autonomous shell/exec/eval**; one `propose_command` escape hatch (exact command → Rohit's Telegram "yes" → `systemd-run` sandbox, no-network, workspace-only); per-wake LLM call/token cap. **② Telegram:** numeric-user-ID allowlist in daemon core *before* the model, ID outside gaffer-writable paths, unknown sender = silent drop + log, 1:1 plain messages only. **③ Secrets:** `LoadCredentialEncrypted` + `/etc/credstore.encrypted` (root 0600), dedicated unprivileged service user, creds only in `$CREDENTIALS_DIRECTORY` tmpfs (never env — kills `/proc/environ` leak), log-redaction as test-asserted invariant, manual scripted rotation, **FPL cookie unprovisioned until actuator proven** (#13/#14). **④ Writes — five tiers, per-(role,path) ACL in daemon code:** 1 core (daemon/ACL/allowlist — Rohit-only via git) · 1.5 instruments (maths `.py` — gaffer edits via offline-selftest-in-sandbox + Rohit "yes") · 2 rulebook (persona/roles/playbooks — propose→"yes") · 3 diary (memory/wiki — distilled words + provenance only, auto-git-commit per wake, post-hoc review) · 4 inbox (`reports/gwNN/` — analysts, own file, write-once, **untrusted forever**); no verbatim persistence of fetched text; analysts can't write memory (poisoned fetch can't self-install); nothing model-writable is executable/daemon-read config (the #9 override-rulebook self-amend path keeps its own AM-gated ACL rule). Skills: **no runtime install ever**, playbooks git-only. Model swap (Qwen etc.) decoupled — injection resistance = future A/B criterion on the OpenRouter seam, not a defense. **⑤ Web:** domain allowlist (seeded from #24 data-sources) + propose-to-add gate, **GET-only**, size caps; residual query-param exfil accepted (no secrets in context). **⑥ Host:** zero inbound (long-poll only), ufw default-deny, SSH key-only, systemd sandboxing (`ProtectSystem=strict`, `NoNewPrivileges`, `MemoryMax` — can't OOM the Pi), avahi kept (fplpi.local), bluetooth/cups off, unattended-upgrades. **⑦ Availability:** 24/7 reaffirmed (power windows rejected — ₹15–25/mo vs missed-lock risk); power-cut fail-safe = auto-boot resume, atomic tmp→fsync→rename state writes, boot catch-up of missed-but-relevant wakes, **deadline guard** (boot inside open lock window → run immediately + alert), SD-corruption hygiene + git off-box backup, **"safe to unplug?" Telegram command** answers from the jobs config. **Deferred:** deploy layout/service user → #11; ACL/redaction/cap implementation → #15/#16; approval-UX formats → #12. Closes the #8 secret-hardening and #9 memory-mechanism deferrals.
- **Repo layout + Pi deploy + supervision locked** ([#11](https://github.com/ropats16/fpl-pi-manager/issues/11) grilling, 2026-08-19) — the "repo = source of truth, Pi pulls" flow made concrete; full build spec in [repo-deploy.md](repo-deploy.md). **Governance = club-owner:** Rohit owns the tier-1 core engine (only he changes it), approves the gaffer's per-GW performance review, breeze-merges its proposed rule/code PRs; the gaffer runs day-to-day, keeps auditable minutes, never touches its own guardrails or acts on the team without a Telegram "yes". **① Tree — organized by who-writes-each-path so the #10 tiers become path-prefix ACL rules:** `daemon/` (tier-1 core) · `pipeline/` (tier-1.5 instruments — **four `.py` moved out of root**, the one refactor forced, #4 re-verified) · `agent/{GAFFER.md,roles/,playbooks/}` (tier-2) · `agent/memory/` (tier-3) · `agent/reports/gwNN/` (tier-4) · `state/season-state.json`; `state/`+`memory/`+`reports/` **committed** (backup/replay/audit rely on it), `data/` gitignored. Tier-1 protection is the **app-level write-ACL** (#10), not filesystem RO — clone stays writable so the pull can update code, but the *model's* tools can't touch tier-1. **② Two branches, path-disjoint:** `main` = clean source of truth (Rohit PRs + gaffer breezed PRs) authors code+rulebook; `pi/live` = daemon-authored runtime `state/`+`memory/`+`reports/`, pushed for off-box backup. Disjoint path-sets → **`git merge origin/main` never conflicts.** The ~2-3×/day audit commits are **squash-compacted per-GW** once the post-GW review clears them (force-push safe on the Pi-only branch) → dozens of commits, not ~500. Findings reach `main` only via PRs or Rohit-curated rulebook edits — raw diary never bulk-merged. **③ Pull (main→Pi) = outbound-only** (no webhook — #10 zero-inbound): an **independent 15-min timer + on-boot** `git fetch && merge origin/main` into `pi/live` (git kept out of the tier-1 daemon; doubles as crash-recovery). Markdown applies next wake (runtime-assembled context), code needs a restart. **④ Gaffer→main = auto-PR:** vetted change on a `gaffer/<slug>` branch off `main` → daemon **auto-opens a PR via `gh`** → Telegram ping → Rohit breeze-merges → pull lands it. **4th secret = a GitHub token** (rides #10 `LoadCredentialEncrypted`), **scope = PR-create + push `gaffer/*` only, no merge** (poisoned gaffer can at most open a PR Rohit rejects); same token does the backup push (one credential, no deploy key). Tier-1 core + transfer-approval gate stay **Rohit-only, immutable.** **⑤ Supervision — two unprivileged `User=gaffer` units** (dedicated user, *not `saf`*), clone `/opt/fpl-gaffer`: `fpl-gaffer.service` (resident daemon, `Restart=always` + **`StartLimitIntervalSec=0`** so a crash-loop keeps cycling until the fix lands, #10 §6 sandboxing, `LoadCredentialEncrypted`, self-schedules wakes from `jobs.yaml`, long-poll Telegram) + `fpl-gaffer-pull.{timer,service}` (the 15-min git oneshot). **Crash-recovery = free:** daemon reads code fresh each start, `Restart=always` already cycles a crashed process → the next restart after the pull lands the fix **self-heals**, no privileged restart. **Deferred:** install/bootstrap + `jobs.yaml` schema → #15; decision-log/approval-UX format → #12; compaction hook → #16; FPL-cookie → #13/#14. Closes the #7/#10 deploy-layout/service-user/pull-cadence deferrals; unblocks #23.
- **Weekly cycle + approval protocol locked** ([#12](https://github.com/ropats16/fpl-pi-manager/issues/12) grilling, 2026-08-21) — the human-loop rhythm, deadline-brief format, and approve/debate/iterate protocol made concrete; full build spec in [weekly-cycle.md](weekly-cycle.md). **Two approval-bearing touchpoints per GW:** a leisurely **DRAFT** brief **IST-pinned to Rohit's evening** (~24–48h out, *not* a raw offset — a raw T−36h lands at 04:30 IST) + a **FINAL ping at true T−2h**; daemon acts T−30..15m (#9). **Every GW runs the ritual, content-scaled** (captaincy is a live write weekly → no true "no-action" GW; a quiet week's draft is two lines). **Brief = lean Telegram** (≤~250 words, phone-readable, headline-first) **+ full `reports/gwNN/decision-log.md` auto-written every GW** (signals/weights/AM-pushback/rejected-alts/provenance — learning-loop baseline #21; depth-on-demand via `why?`). **Draft skeleton (8 parts):** header (GW#, deadline IST+UTC, FT, bank, chips) · **ASK** (transfers→, hits, XI delta, (C)/(VC)) · WHY · Confidence · Dissent (AM's one counter) · Watch · **Contingency** (named "if X ruled out → Y" fallbacks) · reply menu. **Final ping — two forms:** unchanged → notify "locking HH:MM, reply STOP to hold" (opt-out); changed → "⚠ new ASK, reply yes" (opt-in). **Post-lock receipt** confirms the real FPL state. **Approval protocol (gated in daemon code, never model judgment — #10):** ① **token** = whole message (trimmed, case-insensitive) **equals** `{yes, y, lock, approve}` — *not* a substring (`yes but…` → debate), only while state=`AWAITING_APPROVAL(gwNN)`, carry-void resets it (stale scrollback can't fire); ② **plan snapshot** frozen on draft-`yes` = `{transfers, hits, xi, captain, vice, chip, contingencies}`, approval **atomic over the whole plan** (change a field → **iterate** → revised full plan → fresh `yes`); ③ **carry-void = struct diff in daemon code** (any snapshot-field change → fresh `yes`; bench-order/price-only deltas carry silently — materiality is *not* the model's call); ④ **debate unbounded**, gaffer argues with cited evidence + concedes/holds but an explicit **`change X` directive wins** (logged as Rohit's override, scored post-GW — #9 supreme-but-scored, #11 club-owner), ends via `yes`/change→`yes`/timeout; ⑤ **timeout** (no valid approval at act-moment) → run the **matching pre-approved contingency deterministically in code** (stays inside approval-mode — Rohit vetted the exact conditional; an injected model can only *propose* contingencies Rohit approved, never invent a write), else **no-write** (last team stands, FT banks, FPL auto-VC covers, loud alert) — reconciles Rohit's "don't take a zero while asleep" with #10's no-unapproved-write posture; ⑥ **chips never auto-carry** — always a fresh final `yes` (opt-in, irreversible/season-defining) + advance heads-up in prior review/monthly note; ⑦ **post-GW review** (~Mon/Tue after GW settles) emits a lean Telegram review + writes outcomes to `decision-log.md` + opens a `gaffer/*` rulebook PR only on a durable lesson — `yes` there **merges the PR, never touches FPL**, silence leaves it open. **Urgent path:** snapshot-invalidating news fires an immediate off-schedule URGENT ping (same protocol); **quiet-hours 00:00–07:00 IST** hold non-deadline-critical pings till morning, deadline-critical always fires. **Deferred:** exact clock hours (draft IST slot, offset minutes, quiet-hours bounds) → #15 `jobs.yaml`; `AWAITING_APPROVAL` state machine + snapshot-diff + token parser + templating + ≤25k brief-cap assertion → #16; FPL write path the gate sits above → #13/#14; monthly chip/calibration cadence stays #9's. Closes the #9/#10/#11 weekly-cycle/approval-UX/decision-log-format deferrals.
- **Helper fan-out runtime — residuals locked** ([#51](https://github.com/ropats16/fpl-pi-manager/issues/51) grilling, 2026-09-03) — closes the gap that every build ticket (#15–#21) shipped a single-call daemon while #9 had locked the roster; build = [#52](https://github.com/ropats16/fpl-pi-manager/issues/52). Facts established first: `daemon/llm.py` discards the `usage` block (no cost logging exists); the Pi holds only two credentials (OpenRouter, Telegram) — the #11 auto-PR path has zero code and no GitHub token; stdlib GET from the Pi reaches FPL API · Understat (JSON-in-page) · football-data.co.uk CSV · FFS team-news (630 KB HTML), while The Odds API is 401 without a key and FBref/Reddit are 403. **① Model map re-locked:** gaffer `openai/gpt-5.6-sol` ($2/$10 per 1M, live on the Pi since 2026-09-03) · analysts + Scout `z-ai/glm-5.3-flash` ($0.075/$0.25) · **AM `qwen/qwen3.8-max`** ($2/$6) — third family (not OpenAI, not Z.ai) = #9's different-family-judge rationale; **Sonnet 5 rejected** because it is the same family as the dev-time reviewers (Claude Code / Opus 4.8 ad-hoc AM runs) — the runtime judge stays a genuinely fresh family; $6 vs $10 output is secondary; **no one-GW A/B** (n=1 noise) — the #21 review scores AM concessions all season and a swap needs that evidence. All three support `tools` on OpenRouter (verified). **② Which wakes fan out:** DRAFT = full — 4 analysts stepwise at the #12 draft wake (~24–48h out, IST-pinned; **supersedes #9's "~T−72h"** — the Scout's daily log covers the earlier window) → gaffer internal plan (Sol) → AM (Qwen) → gaffer sends the draft with the Dissent slot filled (two Sol calls at draft, +~$0.07/wk); FINAL T−2h = one Scout *delta* pass ("what changed since draft") + the existing Sol final; Scout = own daily 10:00 IST timer appending `reports/gwNN/scout-log.md`; review/chat = no fan-out. Reports enter the draft prompt as **inlined bodies**, capped at write time (analyst ≤700 tok · AM ≤500 · scout-log tail ≤1000 ≈ 4.5k) — base prompt measures ~5–7k so ~18k headroom under the 25k cap; section sits between playbook and memory index in the drop order. **Helpers run one at a time** in the #9 stepwise order (availability → fixtures/odds → quality/style → market), each reading the Scout log + prior reports; draft wake ≈ 25–40 min on the Pi. **③ Fetch = tool loop, not pre-fetch:** helpers get two tools — `fetch(url)` (GET-only, domain allowlist enforced in daemon code at the tool boundary, tag-strip, ~8k-tok per page, per-wake page cache) and `search(query)` (~10 results with excerpts, any domain — **the one explicit amendment to #10 §5**: search excerpts are the only non-allowlisted text that enters helper context; tier-4, never memory; reading a hit still needs the allowlist, else the helper writes a *wanted source: X* line surfaced in the draft's Watch section = the #10 §5 propose-to-add gate). **Search provider = OpenRouter web plugin (Exa)** first — the daemon answers `search()` with one dedicated flash sub-call carrying `plugins:[{id:"web",engine:"exa"}]`, so each search is countable and billed once ($0.007/request, +$0.001 per result past 10), no new key — built as one swappable function; **Brave Search API** ($0.005/query, $5/mo credit, freshness filter, 6th secret) is the documented fallback if the Scout misses recent news. **The Odds API key = 5th secret** via `LoadCredentialEncrypted`, appended by the fetcher only for `api.the-odds-api.com`, never in model context (free 500 req/mo, ~10 used). Seed allowlist by role: availability = FPL flags (code) + FFS team-news/pressers; fixtures/odds = FPL fixtures + Odds API + football-data CSV (closing lines, calibration only); quality = Understat + FPL element-summary; market = FPL API only; Scout = FPL news + FFS; AM = no fetch. **④ Caps = circuit breakers, not leashes** (Rohit's framing: helpers exist to be thorough; ceilings sit ~3× above each role's coverage contract and are *reported*, never silent): per helper **25 fetches · 10 searches · 40 turns · 15 min**; per wake **≤200 LLM calls · ≤5M tokens · ≤$1 est. (usage × price table) · 90 min** (the $ rail bites first at flash prices; call/token numbers are the #10 §1 loop guards); ceilings live in tier-1 config, raised on evidence (`cap_hit` events counted by the #21 review, gaffer may propose). Cap hit → helper is told to write up → report carries *coverage incomplete: X unchecked*. **Failure = degrade, never abort:** fetch error → error text back to the helper; helper crash/timeout → stub report *helper failed: reason, coverage none*; AM down → Dissent reads *AM unavailable*; only the gaffer's own Sol call failing stops a wake (existing retry/alert). Fetch itself is free (own HTTP); only search costs. **Cost per GW under this map** (typical / ceiling-max, per month): gaffer Sol 4 calls/wk $1.2 / $1.2 · analysts flash $0.2 / $1.4 · Scout flash (7/wk + final delta ≈ 35 runs/mo) $0.4 / $2.8 · AM Qwen $0.22 · search Exa (3/helper typ, 10 max, incl. Scout) $1.1 / $3.7 · Odds API $0 → **≈ $3/mo typical ✅, ≈ $9/mo if every helper maxes every ceiling every run** (Scout, being daily, dominates the ceiling case) — so, added under the #8 $5 ceiling (not grilled, veto-able): a **month-to-date ledger**; at $4 MTD helpers drop `search` (fetch-only), at $4.75 helpers stop and the gaffer + AM run alone. Scout's daily search allowance is the swing factor. **⑤ Gaffer proposes a new role — in scope for #52:** the #11 auto-PR path gets built now (GitHub token = 4th secret, scope = PR-create + push `gaffer/*` **and `pi/live`** (the #11 backup push uses the same token — resolves #11's own "gaffer/* only" wording), no merge, `gh` on the Pi, `gaffer/<slug>` branch off `main`, Telegram ping, Rohit breeze-merges); a role proposal = a drafted `agent/roles/<slug>.md` + evidence (cap_hit pattern, uncovered axis) landing as a PR. **Deferred:** none — #52 is unblocked.

## Not yet specified

Fog (can't phrase sharply yet — everything sharp is now a ticket on the tracker):

- **Phase 2 — gaffer visualizer front-end**: pitch board with team, gaffer avatar center, background agents at desks (Rohit's reference image). Single-vs-multi resolved by #9 (2026-08-18) — there IS a multi-role roster to visualize (gaffer + 4 analysts + Scout + AM at desks); now waits only on Rohit walking through the concept; likely a /prototype ticket then.
- **Phase 3 — web playable football game** (shareable, play vs others): waits on landscape research; likely its own wayfinder effort after this map closes.
- Learning loop (post-GW review grading, learnings log feeding prompts/skills).
- **Wiring #24 + #25 into the real gaffer** — the [#24 research wiki](research/team-selection/index.md) is the gaffer's standing *briefing*; the [#25 decision-log](gw1/decision-log.md) format is the *learning-loop training record* (per-GW emit); the [optimizer-as-one-voice / tight-ship anti-fabrication / observer-pushback](gw1/approach.md) patterns are reusable behaviors. *(The 2026-08-17 fresh-discovery caveat was satisfied by #9's 3-track research pass, 2026-08-18; the Scout role fog was resolved by #9 — distinct daily role. Wiring itself is #16/#12 build work.)*
- **Multi-week + chip foresight** — the #25 GW1 run was single-GW-optimized (kept price-point flexibility + named GW2+ upgrade paths, forecloses no chip, but did NOT run a 5-6 GW fixture-swing plan). The daemon needs the rolling multi-week horizon and chip-*timing* layer (below); the *machinery* is now specified — MILP formulation, MC-over-fixture-scenarios, cadence — in [#9's season-long research](research/gaffer-architecture/raw/season-long-machinery.md); building it remains.
- Chip strategy engine (8 chips, set-1 expiry GW19 / 2 Jan 2027).
- Price-watch alerts (livefpl.net = React SPA, needs XHR sniffing).
- Backtest harness for ad-hoc questions (e.g. GK+DEF same-team doubling).
- v2 projections model (Understat blend, odds layer, new-signing minutes).

## Out of scope

- Executing the blueprint's build stages — separate effort after this map closes (except the GW1 carve-out tasks above).
- Sentiment analysis — cut by prior spec, uncontested.

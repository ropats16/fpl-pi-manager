# Watch shortlist

Players NOT in the squad whose price/status moves should still trigger a watch
alert (#17) — transfer targets, next-GW candidates. One FPL `web_name` per
line (bullets optional, case-insensitive); full-line `#` comments are ignored.
Squad players are always watched automatically from season-state; never list
them here. Edits apply on the next watch wake via the normal pull — no restart.

# - Salah
# - Saka

# Learnings log
#
# The gaffer's running diary of lessons distilled from ad-hoc analyses (#20).
# Model-written, tier-3 (distilled words + provenance only — never raw fetched
# text, never a link), and **append-only**: the daemon appends vetted entries
# from the ```learnings block at the end of an analysis reply. Never edit,
# reorder, or delete a past entry — this is the record of what was believed and
# when. Standing lessons that held up get promoted to `MEMORY.md` by the post-GW
# review; this file keeps the raw stream.
#
# One entry per line, exactly:
#   - [YYYY-MM-DD] [GWnn] [specific|general] <lesson> — evidence: <evidence> — q: <question>
#
# `specific` = this squad/these players/this season; `general` = a reusable rule.
# " — " is the field separator and appears NOWHERE else in a line (use " - "
# inside a field). `[GW??]` when the gameweek is unknown. The parser is a strict
# regex on `- [` lines — every other line, this header included, is ignored.
# Prompt assembly injects a bounded, relevance-scored selection of these entries
# as evidence, not instructions.

- [2026-08-28] [GW02] [general] With £0.0 in the bank a target's price rise locks the move out permanently, so burn the FT rather than roll whenever the roll's option value is ~nil - next week's 2 FT would have bought the same move plus one more anyway. — evidence: GW2 decision log (plans/gw2), 2026-08-28 - Yates 4.5 to Slater 4.5 adjudicated FOR burn at £0.0 bank, 1 FT. — q: burn the FT or roll?
- [2026-08-28] [GW02] [specific] Haaland's captain weeks in this run are the home ones - GW3 (COV) and GW5 (SUN); away at CRY was his weakest fixture of the run and both models edged Bruno, so the armband went to Bruno. — evidence: GW2 decision log (plans/gw2), 2026-08-28 - repo projections 4.75 v 4.00, RotoWire 6.40 v 6.26, MUN home to promoted IPS and on pens. — q: captain Bruno or Haaland GW2?
